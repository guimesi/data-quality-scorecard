# Local development + GitHub CI dependencies (PyPI).
# Production (Streamlit in Snowflake) deps are declared separately in
# environment.yml (Snowflake Anaconda channel).
#
# Python >= 3.10 required: the security-fixed Streamlit (>=1.54) needs 3.10+;
# CI and SiS run Python 3.11.
streamlit>=1.54.0,<2.0
pandas>=2.0.0,<3.0
numpy>=1.24.0,<3.0
snowflake-connector-python>=3.6.0,<5.0
plotly>=5.18.0,<7.0
python-dotenv>=1.2.2,<2.0
pytest>=8.3.4,<10.0
pytest-cov>=4.1.0,<8.0
# Optional: enables IsolationForest / KMeans / PCA / LogisticRegression in
# Step 7: ML Lab. The lab works without it (numpy fallbacks), but a
# scikit-learn install unlocks a "🔬 Use scikit-learn" toggle.
scikit-learn>=1.3.0,<2.0
