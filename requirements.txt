# Application dependencies (PyPI). Used by local development, GitHub CI
# and the Databricks Apps runtime (Apps installs requirements.txt from the
# repo root into its container).
#
# Python >= 3.10 required: the security-fixed Streamlit (>=1.54) needs 3.10+;
# CI runs Python 3.11 (same major line as the Databricks Apps runtime).
streamlit>=1.54.0,<2.0
pandas>=2.0.0,<3.0
numpy>=1.24.0,<3.0
# Databricks SQL Warehouse access (Arrow fetch path) + unified headless
# auth (app service principal in Databricks Apps, PAT locally).
databricks-sql-connector>=3.7.0,<5.0
databricks-sdk>=0.30.0,<1.0
plotly>=5.18.0,<7.0
python-dotenv>=1.2.2,<2.0
# Makes SSL use the OS trust store so outbound HTTPS (Airtable push) works
# behind corporate TLS-inspecting proxies; injected defensively in app.py.
truststore>=0.9.0,<1.0
pytest>=8.3.4,<10.0
pytest-cov>=4.1.0,<8.0
# Optional: enables IsolationForest / KMeans / PCA / LogisticRegression in
# Step 7: ML Lab. The lab works without it (numpy fallbacks), but a
# scikit-learn install unlocks a "🔬 Use scikit-learn" toggle.
scikit-learn>=1.3.0,<2.0
